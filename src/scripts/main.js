/**
 * Sample react.js comments application
 * @author juanvallejo123
 */

// import deps
var Application = require('./Application.jsx');

/**
 * Renders the root component and all child nodes
 * once the page has loaded
 */
window.addEventListener('load', function() {
	Application.render();
});

